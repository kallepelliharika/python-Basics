class Solution(object):
    def canJump(self, nums):
        maxIndex=0
        for ind in range(0,len(nums)):
            if(ind>maxIndex):
                return False
            maxIndex=max(maxIndex,ind+nums[ind])
        return True
